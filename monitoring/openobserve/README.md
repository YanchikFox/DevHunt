# OpenObserve

Unified logs/traces/metrics backend for DevHunt.

| | |
|---|---|
| URL | `http://localhost:5080` |
| Login | `root@devhunt.com` |
| Password | `devhunt123` |

Alert rules are defined in `monitoring/alerts.yml` and applied automatically by the `openobserve-init` container on first startup.
